# 2026-03-08

- Upgraded OpenClaw from 2026.3.2 to 2026.3.7 via `openclaw update`; gateway needed restart afterward.
- Tightened three X reply cron pipelines to stricter target rules: within 2 hours and views > 5000, with retry/refresh loops if no qualified target found.
- `x:timeline-new-playwright` cron interval changed from 30 minutes to 20 minutes. Runner timeout restored to 20 minutes after briefly changing the wrong knob.
- `timeline-new-playwright` picker language rule added: 03:00-07:59 Asia/Shanghai replies only to English tweets; all other times reply only to Chinese tweets.
- Fixed `x_timeline_new_playwright.sh` posting robustness: retries up to 3 times on transient Playwright/browser transport failures like EPIPE / browser closed.
- Investigated a failed reply to `XianyuLi/status/2030192629900792291`: reply generation succeeded, posting failed in `PostTweet_Reply_PW.py` with Playwright `write EPIPE`; no success log entry was written.
- `x_viral_sopilot_reply.sh` fallback to legacy script was removed; if sopilot targets are unavailable it now exits 75 and skips the round.
- `sopilot_hot_tweets.py` was rewritten from flaky `agent-browser` scraping to direct Playwright scraping; verified working again and writing valid `data/sopilot_hot_targets.json`.
- Added age parsing (`age_h`) to sopilot targets and enforced `x_viral_sopilot_reply.sh` to only pick tweets within 2 hours.
- Found root cause of `x:viral-interact` silence: cron delivery mode was `none`, and the script’s age check frequently failed to `99999min` because it reopened the tweet page to fetch `<time>`. Fixed by making `find_viral_tweets.py` capture `time`, `ageMin`, `handle`, `views`, and `likes` directly from the home timeline and making `x_viral_interact_agent.sh` consume `ageMin` from `data/viral_targets.json` instead of reopening the tweet page.
- Changed `x:viral-interact` cron delivery to Discord announce mode (`channel:1476191544808837192`) so future runs report status again.
- SSH hardening changed over the day: root login disabled, only user `ls` allowed, password login ultimately disabled again, and `ls` set to public-key-only login.
- Synced root’s existing ED25519 public key into `/home/ls/.ssh/authorized_keys` so the same private key used for root can log in as `ls`.
- Diagnosed one SSH access issue as sshguard firewall blocking the user’s public IP; added the IP to `/etc/sshguard.whitelist` and unblocked it.
- Workspace cleanup was performed conservatively by moving unused files/directories into `.trash/...` rather than hard deleting; accidentally moved `articles/` once, then restored it and marked it as a must-keep directory because it stores draft Twitter articles.
- Rebang task improvements: `rebang_home_hot_image_tweet_pw.sh` now uses title-only text if body extraction fails but image exists, uses title + body if body exists, and uses a sub-agent to condense overlength tweet text to <=140 chars without hard truncation; if condensing still fails, it skips the round.
- Fixed a bug in rebang condense result parsing: the script had been piping JSON into `python3 - <<'PY'`, causing empty stdin and false “condense failed” results; changed parsing to pass JSON via environment variable.
- SSH brute-force monitoring summary around the afternoon/evening: attacks continued steadily from many IPs, but successful logins observed were only the user’s own `ls` sessions (password first, then publickey). 
