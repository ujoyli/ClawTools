# Memory flush — 2026-03-09 (Asia/Shanghai)

Summary of events and durable notes captured on 2026-03-09:

- Cron pipelines status:
  - rebang_home_hot_image_tweet_pw.sh, x_viral_interact_agent.sh, and x_viral_sopilot_reply.sh ran throughout the day and are generally healthy.
  - Many successful postings and replies recorded; scripts commonly returned {"ok": true, "url": "https://x.com/home"} and marked sent.

- Notable successful posts/replies (examples):
  - Multiple rebang posts from Hupu with images posted and marked sent (titles and tmp images saved in workspace/tmp/).
  - x_viral_interact posted replies to many viral X targets (examples: tim_cook, elonmusk, MrBeast, CENTCOM, etc.).
  - x_viral_sopilot_reply saved Top10 hot targets repeatedly to data/sopilot_hot_targets.json and generated multiple replies.

- Persistent issues observed (need follow-up):
  1. Intermittent SIGTERM killing some runs mid-post (observed on x_viral_interact and x_viral_sopilot_reply). Likely causes to investigate: cron/runner timeout, system OOM, or external supervisor.
  2. Occasional condense_text / subagent failures causing exit code 1 during rebang flow; one concrete case on March 6th produced a failure during condense step.
  3. Python warning at line 62 observed in runtime — locate exact file and warning text and fix.
  4. Playwright Page.goto timeouts (e.g., Timeout 70000ms exceeded) occasionally causing failures.
  5. One Codex/server error returned during a rebang run (request ID captured in logs) — transient but monitor.
  6. Browser/Chromium process count spikes sometimes; recommended to monitor and cap concurrent browsers.

- Decisions / protective behaviors established:
  - When sopilot refresh returns "No tweets found", scripts fallback to existing targets file.
  - Scripts abort posting when source has no body or no media (exit codes 76 and skips respectively).
  - x_viral_interact uses selection rules: age <= 120 minutes and views > 5000 (max 5 tries).
  - rebang_home_hot_image_tweet_pw.sh attempts up to 10 tries per run.
  - Posts/replies recorded to local persistence: data/x_reply_log.jsonl, data/x_replied_targets.json, data/sopilot_hot_targets.json, and rebang_sent.db.

- Pending action items (prioritized):
  1. Reproduce and diagnose SIGTERM: run x_viral_interact with bash -x and long timeout; collect process list, dmesg OOM logs, and check cron/runner policies.
  2. Investigate condense_text/subagent failure: locate scripts invoking subagent, re-run with -x, capture subagent stdout/stderr.
  3. Fix Python warning at line 62: identify file and patch; add unit test if needed.
  4. Tune Playwright timeouts (increase page.goto timeout), add retries, and improve handling for twc-cc-mask cookie consent overlay.
  5. Add process/watchdog to limit Chromium instances and restart BrowserWing when CDP port unresponsive.

- Operational notes for next runs:
  - Recommended debug commands were prepared (bash -x runs saving logs to /root/.openclaw/workspace/logs/). Use timeout 4500s for long runs.
  - For immediate progress without granting exec to the assistant, use "Draft-only" workflow: assistant will produce ready-to-post replies for pending targets.

- Misc:
  - Workspace paths with important files: scripts/, data/, tmp/, logs/, memory/. Keep backups before deleting.
  - Posting interface: x-cdp reply-tweet.js on port 44407.


Additional user preferences recorded (2026-03-09 20:03 Asia/Shanghai):

- From 大帅: Do NOT use the bot to like posts. (No automated likes by the bot.)
- Picker rules updated by user: raise the views threshold (user allows higher requirement) but keep the time window at 2 hours (age <= 120 minutes).


End of 2026-03-09 memory flush.
