
## 2026-02-27 11:05 (X CDP automation + viral replies)
- Switched X/Twitter posting from official API to **CDP browser automation** using installed skill `x-cdp`.
- Chromium headless running with CDP on **port 18802** using profile `/root/profiles/x-cdp`.
- Cookies injected from `/root/cookie.json`; CDP connectivity verified via `curl http://localhost:18802/json/version`.
- Confirmed CDP posting works: `post-tweet.js` succeeded and visible on timeline.
- Implemented high-frequency reply automation:
  - Cron job `x:fast-viral-reply` now runs **every 2 minutes** (`*/2 * * * *`, Asia/Shanghai) with 0–20s jitter; delivery disabled to avoid spam.
  - Replies are generated via **OpenClaw agent** (`openclaw agent --session-id x-fast-reply --json ...`), not fixed templates; language matches tweet (Chinese->Chinese, else English).
  - Fixed a parsing bug where agent output existed but was parsed as empty; now agent JSON is written to `/tmp/x_agent_reply.json` and parsed from file.
  - Added dedupe to avoid replying to same status within 24h using `data/x_replied_targets.json`.
  - Evidence log appended to `data/x_reply_log.jsonl` with `ts`, `target_url`, `tweet_text`, `reply_text`.
- Summary cron `x:summary-report` remains every 30 minutes with Discord delivery; fast reply cron delivery is off.
- User feedback: do not spam per-success reports; maintain ~1–2 minute reply cadence with randomness; ensure no English replies under Chinese tweets; if failures occur, agent should self-diagnose and self-fix without asking.

## 2026-02-27 17:55 (回复策略升级)
- 回复频率从 **每2分钟** 改为 **每小时1次**（cron: `0 * * * *`）
- 新增爆款挖掘脚本 `scripts/sopilot_hot_tweets.py`：从 sopilot.net/zh/hot-tweets 抓取起爆帖
  - 按起爆概率 ≥50%、预测浏览量 ≥1万 筛选
  - 按预测浏览量降序排列
  - 72h 去重窗口
- 回复脚本从 `x_viral_fast_reply.sh` 升级为 `x_viral_sopilot_reply.sh`
  - 先刷新 sopilot 目标，再选最高预测浏览量的未回复推文
  - 回复提示词要求有观点、有态度、能引发讨论
- `post-tweet.js` 增加 API 视频上传 fallback（CDP 视频失败时自动走 API）
- Cookie/登录状态问题：cookie.json 失效，需大帅重新导出

## 2026-02-27 19:15 (视频发推修复 + 抖音热榜流程)

### 视频发推问题根因与修复
- **根因**: headless Chrome 没有视频编解码器，X 前端预览失败导致视频上传静默失败（显示"无法播放该媒体"）
- **修复**: `post-tweet.js` 改为视频直接走 Twitter API（`video_post.js`），不再尝试 CDP 上传视频。图片/纯文字继续走 CDP
- 测试了 4 种 CDP 视频上传方法（uploadFile、setFileInputFiles、DataTransfer、fileChooser），全部失败，确认是 headless Chrome 根本性限制

### Cookie 更新
- 大帅提供了新 cookie 文件 `/root/x_cookies.json`，已复制到 `/root/cookie.json`
- 14 个 cookie，auth_token/ct0/twid 齐全
- 注入 Chromium CDP 成功，登录状态恢复

### 爆款挖掘系统（sopilot）
- `scripts/sopilot_hot_tweets.py` — 从 sopilot.net/zh/hot-tweets 抓取起爆帖（起爆概率≥50%，预测浏览量≥1万，72h去重）
- `scripts/x_viral_sopilot_reply.sh` — 替代旧回复脚本，流程：刷新sopilot→选最优→agent生成回复→CDP发布
- cron `cd658133` 改为每小时执行 `x_viral_sopilot_reply.sh`

### 抖音热榜视频发推流程
- `rebang_douyin_video_post.py` 存在但 rebang.today 超时打不开
- 替代方案：agent-browser 直接打开 douyin.com/hot 抓取热榜视频链接
- TikTokDownloader 工具可以下载（yt-dlp 不行，需要登录 cookie）
- 下载目录：`tmp_tiktokdownloader/Volume/Download/`
- 成功发布：非遗8秒视频 → https://x.com/i/status/2027335851508945253

### 关键路径
- 视频发推：抖音热榜 → TikTokDownloader下载 → `video_post.js` API上传 ✅
- 文字回复：sopilot爆款 → agent生成 → CDP发布 ✅
- rebang.today 目前不可用（超时）
